# 綁定的 IP 地址
bind 0.0.0.0

# 使用的端口
port 6380

# 快照文件名稱
dbfilename dump.rdb

# 快照文件儲存目錄
dir /data

# 關閉 AOF 持久化，因為默認是關閉的
appendonly no

# 設置最大客戶端連接數
maxclients 1000

# 設置最大使用內存
maxmemory 256mb

# 超過記憶體限制時的數據淘汰策略
maxmemory-policy allkeys-lru
